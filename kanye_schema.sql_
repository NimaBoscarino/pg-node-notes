CREATE TABLE artists (id SERIAL UNIQUE PRIMARY KEY,name varchar(90));
CREATE TABLE lyrics (id SERIAL UNIQUE PRIMARY KEY,text varchar(90));
CREATE TABLE songs (id SERIAL UNIQUE,artist_id SERIAL REFERENCES artists,lyric_id SERIAL REFERENCES lyrics);